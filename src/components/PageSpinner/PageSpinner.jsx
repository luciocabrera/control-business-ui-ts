// PropTypes
import { Overlay, Spinner } from 'components';
// Components

const PageSpinner = () => (
  <Overlay>
    <Spinner id="page-spinner-styled" />
  </Overlay>
);

export default PageSpinner;
